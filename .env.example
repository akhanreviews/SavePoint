# Port to open on the Docker host.
APP_PORT=8080

# Optional: set this to the host's Tailscale IP to bind only on Tailscale.
# Leave it unset to listen on every host interface, including Tailscale.
# Example:
# TAILSCALE_BIND=100.64.0.10
